<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Feed</title>

    <link rel="stylesheet" href="/style.css">

  </head>
  <body>

    <div id="container">

      <% include ../views/partials/header.ejs %>


      <% for (let i=0; i < allPhotos.length; i++) { %>
        <% for (let j=0; j < allUsers.length; j++) { %>
        <a href='/users/<%=allUsers[j].username%>/<%= allPhotos[i].id %>'><img src="<%= allPhotos[i].url %>"></a>
        <% } %>
    <% } %>

    </div>

  </body>
</html>
